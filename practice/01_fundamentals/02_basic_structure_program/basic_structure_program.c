/*
    Estrutura básica de um programa em C
*/

// Todo programa em C é obrigatório ter a função main.
// É a partir dela que o compilador inicia a execução do programa.
int main(){ // As funções em C possuem tipos de retorno definidos.
            // Abordaremos esse conceito com mais detalhes posteriormente.
    // Exemplo de uma soma simples
    int a = 20;
    int b = 20;
    return a + b;
}